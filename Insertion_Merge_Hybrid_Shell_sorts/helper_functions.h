#include <vector>

void merge(std::vector<int>& nums,std::vector<int>& v1,std::vector<int>& v2);
//void addVector(std::vector<std::vector<int> >& nums, int arr[]);
void printVector(std::vector<int>& nums);